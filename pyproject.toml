[project]
name = "devis-lab"
version = "0.1.0"
description = "Lab IA-natif LMFR — feature de simulation devis-pose"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]
markers = [
    "eval: test eval dérivé de spec.md §7 — merge gate (make evals)",
]
