# Dependency allowlist — the ONE sanctioned home for third-party package names
# (lab/engine/dep_guard.py, `just check-deps`, wired into gate/gate-ci).
#
# Adding a dependency = adding its name here IN THE SAME COMMIT, after a human
# verified on PyPI that the package exists under this exact spelling (anti
# slopsquatting — the guard itself stays offline and deterministic).
# One name per line, comments with #. Names are PEP 503-normalized on load.

pytest
ruff
